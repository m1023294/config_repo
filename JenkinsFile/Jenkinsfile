pipeline {
    agent any
    stages {
		stage('clone') {
			steps {
				script {
					git url: 'git://github.com/m1023294/util_repo',
                    branch: 'master'
					echo 'clone success'
					load '/java/java-ci'
					echo 'load success'
				}
			}
		}
	}
}